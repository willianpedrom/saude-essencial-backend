# Variáveis de ambiente – Saúde Essencial Backend
# Copie este arquivo para .env e preencha os valores

# Banco de dados (Railway fornece automaticamente)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# JWT (gere uma string aleatória longa e segura)
JWT_SECRET=coloque_uma_string_secreta_longa_aqui_abc123

# Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_STARTER=price_...
STRIPE_PRICE_PRO=price_...
STRIPE_PRICE_PREMIUM=price_...

# URL do frontend (sem barra no final)
FRONTEND_URL=http://localhost:8080

# Ambiente
NODE_ENV=development
PORT=3001

# ─── Email transacional (Nodemailer) ───────────────────────────────────────────
# Use Gmail, SendGrid, Brevo, Zoho, Mailgun, etc.
# Opção 1 — Gmail (use senha de app, não a senha normal):
#   SMTP_SERVICE=gmail
#   SMTP_USER=seuemail@gmail.com
#   SMTP_PASS=xxxx xxxx xxxx xxxx  ← senha de app Google
# Opção 2 — SMTP genérico (SendGrid, Brevo, etc.):
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=seuemail@gmail.com
SMTP_PASS=sua_senha_de_app_aqui
SMTP_FROM="Gota Essencial <seuemail@gmail.com>"

# URL pública do sistema (usada no email de boas-vindas)
PLATFORM_URL=https://gotaessencial.com.br
